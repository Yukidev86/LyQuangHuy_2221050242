@model List<DemoMvc.Models.DonHang>

<table>
@foreach(var item in Model)
{
    <tr>
        <td>@item.Id</td>
        <td>@item.KhachHang.TenKhachHang</td>
        <td>
            <a asp-action="Details" asp-route-id="@item.Id">Chi tiết</a>
        </td>
    </tr>
}
</table>